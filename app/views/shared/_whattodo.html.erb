<% unless @user.profile_finished? %>
  <div class="pr-cont-header">
    <h1>Building your showcase</h1>
  </div>
  <div class="pr-whattodo-container">
    <div class="whattodo-header">
      <% if @user.profile_empty? %>
        <h2>Welcome to freeme! Let's get you started.</h2>
      <% else %>
        <h2>Great start. Keep going!</h2>
      <% end %>
    </div>
    <% if @skills != nil %>
      <% if @skills.length < 1 %>
        <div class="whattodo-div">
          <h2>🤐 Add some skills</h2>
          <h3>Add some of the amazing skills you've learned during your bootcamp.
              click on the plus button and add your first skill.</h1>
        </div>
      <% end %>
      <% if @skills.length == 1 %>
        <div class="whattodo-div">
          <h2>🧐 Add some more skills</h2>
          <h3>Keep adding skills. You can never have too many.</h1>
        </div>
      <% end %>
    <% end%>
    <% if @projects!= nil %>
      <% if @projects.empty? %>
        <div class="whattodo-div">
          <h2>💪 Add some projects</h2>
          <h3>Show some of the projects that you've worked on.
              Big or small.</h1>
        </div>
      <% end %>
      <% if @projects.length == 1 %>
        <div class="whattodo-div">
          <h2>😎 Add some more projects</h2>
          <h3>You can even add that css race game you made back in week 3 of your bootcamp.</h1>
        </div>
      <% end %>
    <% end %>
    <% if @experiences != nil %>
      <% if @experiences.empty? %>
        <div class="whattodo-div">
          <h2>👢 Add some experience</h2>
          <h3>Finishing a bootcamp is a massive achievement. Add it now!</h1>
        </div>
      <% end %>
      <% if @experiences.length == 1 %>
        <div class="whattodo-div">
          <h2>🚀 Add some more experience</h2>
          <h3>Doesn't even have to be code related. All experience counts!</h1>
        </div>
      <% end %>
    <% end %>
  </div>
<% end %>
